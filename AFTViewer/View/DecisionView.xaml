<UserControl x:Class="AFTViewer.View.DecisionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AFTViewer.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
        <Button x:Name="ValidateButton" Content="{Binding ValidateButtonContent}" Margin="4" Padding="8,4" Click="ValidateFailure_Click" FontSize="14" FontWeight="Bold" Background="{Binding ValidateButtonBackground}" Foreground="White"/>
        <Button x:Name="FalsePositiveButton" Content="{Binding FalsePositiveButtonContent}" Margin="4" Padding="8,4" Click="FalsePositive_Click" FontSize="14" FontWeight="Bold" Background="{Binding FalsePositiveButtonBackground}" Foreground="White"/>
        <Button Content="Remplacer la spécification" Margin="4" Padding="8,4" Click="OverrideSpec_Click" FontWeight="Bold" FontSize="14" Background="Gray" Foreground="White"/>
    </StackPanel>
</UserControl>
